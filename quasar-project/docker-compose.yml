version: '3.8'

services:
  quasar-app:
    container_name: personal_page
    build: .
    ports:
      - "8081:8081"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - HOST=0.0.0.0
    command: quasar dev --port 8081
